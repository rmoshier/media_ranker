<h1>All Movies</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Director</th>
    <th>Description</th>
  </tr>

  <% @movies.each do |movie| %>
    <tr>
      <td><%= movie.name %></td>
      <td><%= movie.director %></td>
      <td><%= movie.description %></td>
    </tr>
  <% end %>
</table>
